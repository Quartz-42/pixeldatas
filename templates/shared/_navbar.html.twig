<nav class="bg-gray-900 text-amber-400">
	<div
		class="flex items-center justify-between p-2 mx-auto">
		<!-- Logo + Accueil -->
		<div class="flex items-center ml-5 md:ml-10">
			<a href="{{ path('app_home') }}" class="flex items-center retour-lien">
				<img src="{{ asset('img/icones-d-eclair-jaune.png') }}" class="h-10 mt-2" alt="Logo">
				<span class="text-xl font-semibold whitespace-nowrap dark:text-white ml-2">Accueil</span>
			</a>

			<!-- Menu Desktop -->
			<div class="hidden md:flex items-center ml-5 md:ml-10">
				<ul class="flex">
					<li>
						<a href="{{ path('app_all_pokemon') }}" class="text-xl font-semibold retour-lien p-3 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-white dark:text-white md:dark:hover:text-blue-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent">
							Pokedex
						</a>
					</li>
				</ul>
			</div>
		</div>

		<!-- Bouton Menu Mobile -->
		<div class="md:hidden ml-auto">
			<button id="menu-toggle" type="button" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-amber-400 rounded-lg hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-amber-400 dark:hover:bg-gray-700 dark:focus:ring-gray-600">
				<span class="sr-only">Open main menu</span>
				<svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 0 17 14">
					<path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
				</svg>
			</button>
		</div>
	</div>

	<!-- Menu Mobile (Pokedex uniquement) -->
	<div id="mobile-menu" class="hidden transition-all duration-300 ease-in-out md:hidden px-4 pb-4">
		<ul class="flex flex-col space-y-4 text-right">
			<li>
				<a href="{{ path('app_all_pokemon') }}" class="retour-lien py-2 px-3 rounded hover:bg-gray-100 dark:text-white dark:hover:bg-gray-700 uppercase">
					Pokedex
				</a>
			</li>
		</ul>
	</div>
</nav>

<script>
	document.getElementById('menu-toggle').addEventListener('click', function () {
const menu = document.getElementById('mobile-menu');
menu.classList.toggle('hidden');
});
</script>
