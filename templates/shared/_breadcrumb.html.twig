{# <div class="max-w-xl px-4 mx-auto lg:max-w-screen-xl lg:px-8 md:max-w-3xl sm:px-12">
    <div class="order-1 md:order-2 my-6">
        <nav aria-label="breadcrumb" class="flex items-center align-center text-[15px]">
            <span>
                <a href="/" class="flex items-center text-gray-500 no-underline transition duration-300 ease-in-out hover:text-gray-900 hover:no-underline group">
                    <svg xmlns="http://www.w3.org/2000/svg" class="flex-shrink-0 w-[1.125rem] h-[1.125rem] text-gray-400 transition duration-300 ease-in-out group-hover:text-gray-500" viewbox="0 0 20 20" fill="currentColor">
                        <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"/>
                    </svg>
                </a>
            </span>
            <span class="text-gray-400">
                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-gray-400" viewbox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
                </svg>
            </span>

            {% set linkType =
                article is defined ? path('show_type', {'type': article.type.value}) :
                category is defined ? path('show_category', {'slug': category.slug}) :
                path('show_type', {'type': type.value})
            %}

            {% set type =
                 article is defined ? article.type.value :
                 category is defined ? category.name :
                 type.value
            %}

            {% set linkCategory =
                    article is defined ? path('show_category', {'slug': article.category.slug}) :
                    category is defined ? path('show_category', {'slug': category.slug}) :
                    path('show_type', {'type': " "})
            %}

            {% set category =
                    article is defined ? article.category.name|capitalize :
                    category is defined ? category.name|capitalize
            %}

            <span class="text-red-700">
                <div class="flex align-center item-center text-[15px]">
                    <a href="{{ linkType }}">
                        {% if linkType != linkCategory %}
                            {{ ('type.' ~ type ~ '.title')|trans }}
                        {% elseif category is defined %}
                            {{ category }}
                        {% endif %}
                    </a>
                    {% if linkType != linkCategory and article is defined %}
                        <span class="text-gray-400">
                            <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-gray-400" viewbox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
                            </svg>
                        </span>
                        <a href="{{ linkCategory }}">
                            {{ category }}
                        </a>
                    {% endif %}
                    {% if article is defined %}
                        <span class="text-gray-400">
                            <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-gray-400" viewbox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
                            </svg>
                        </span>
                        <span class="text-black">
                            {{ article.title }}
                        </span>
                    {% endif %}
                </div>
            </span>
        </nav>
    </div>
</div> #}
